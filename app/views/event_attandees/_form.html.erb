<%= form_with(model: [event_attandee.event, event_attandee], id: 'event-attandee-form') do |form| %>
  <% if event_attandee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_attandee.errors.count, "error") %> prohibited this event_attandee from being saved:</h2>

      <ul>
      <% event_attandee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :status %>
    <%= form.text_field :status %>
  </p>

  <p>
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </p>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
